Read .agent/README.md first and follow the CSS Styling Architecture index

Use existing CSS docs in .agent/CSS/ as context for styling work

Consolidate: update existing CSS docs before creating new ones

When CSS docs change, update .agent/README.md to keep the index accurate

Log style-impacting changes in CSS/changelog.md

New CSS docs must include a Related Documentation section

Scope is visual only (layout, tokens, components, theming); ignore logic and backend